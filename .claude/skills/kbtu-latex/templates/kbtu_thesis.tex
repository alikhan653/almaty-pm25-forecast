% ============================================================
%  KBTU Master's Thesis — XeLaTeX template
%  Standard: GOST 7.32-2017 + KBTU 7M06106 requirements
%  Engine:   XeLaTeX  (required for Times New Roman + Unicode)
%  Compile:  xelatex → biber → xelatex → xelatex
% ============================================================

\documentclass[14pt, a4paper, oneside]{extreport}

% ── Encoding & language ──────────────────────────────────────
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{english}
\setotherlanguages{russian, kazakh}

% ── Fonts: Times New Roman (GOST requirement) ────────────────
\setmainfont{Times New Roman}
\setsansfont{Arial}
\setmonofont{Courier New}

% ── Page geometry (GOST 7.32 + KBTU) ────────────────────────
%   left 30 mm | right 10 mm | top 20 mm | bottom 20 mm
\usepackage[
  a4paper,
  left=30mm, right=10mm,
  top=20mm,  bottom=20mm
]{geometry}

% ── Line spacing: single (одинарный) ─────────────────────────
\usepackage{setspace}
\setstretch{1.0}

% ── Paragraph indent 1.25 cm; no extra space between paras ───
\usepackage{indentfirst}
\setlength{\parindent}{12.5mm}
\setlength{\parskip}{0pt}

% ── Page numbers: bottom centre, 12 pt ───────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\fontsize{12}{14}\selectfont\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
% Title/abstract pages: counted but number NOT printed
\fancypagestyle{nonum}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0pt}%
}

% ── Heading styles ───────────────────────────────────────────
\usepackage{titlesec}

% Level 1 — CHAPTER: 16 pt, bold, centred, ALL CAPS, new page
\titleformat{\chapter}[block]
  {\newpage\centering\fontsize{16}{18}\selectfont\bfseries\MakeUppercase}
  {\thechapter.}{1em}{}
\titlespacing*{\chapter}{0pt}{0pt}{12pt}

% Level 2 — Section: 14 pt, bold, left-aligned
\titleformat{\section}[block]
  {\fontsize{14}{16}\selectfont\bfseries}
  {\thesection.}{0.5em}{}
\titlespacing*{\section}{0pt}{10pt}{4pt}

% Level 3 — Subsection: 14 pt, bold italic, left-aligned
\titleformat{\subsection}[block]
  {\fontsize{14}{16}\selectfont\bfseries\itshape}
  {\thesubsection.}{0.5em}{}
\titlespacing*{\subsection}{0pt}{8pt}{2pt}

% No deeper than level 3
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

% ── Table of contents ─────────────────────────────────────────
\usepackage{tocloft}
\renewcommand{\cftchapfont}{\bfseries}
\renewcommand{\cftchappagefont}{\bfseries}
\renewcommand{\cftchapdotsep}{\cftdotsep}

% ── Tables ───────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{caption}
% Table captions: ABOVE, left-aligned, "Table X.X – Title"
\captionsetup[table]{
  position=above,
  justification=raggedright,
  singlelinecheck=false,
  labelsep=endash,        % em-dash separator
  font={normalsize},
  skip=4pt
}
% Figure captions: BELOW, centred, "Figure X Title" (no dash)
\captionsetup[figure]{
  position=below,
  justification=centering,
  labelsep=space,
  font={normalsize},
  skip=4pt
}

% ── Figures ──────────────────────────────────────────────────
\usepackage{graphicx}
\graphicspath{{figures/}{../figures/}}

% ── Equations ────────────────────────────────────────────────
\usepackage{amsmath, amssymb}
% Equation numbers: (chapter.eq)
\numberwithin{equation}{chapter}

% ── Code listings ────────────────────────────────────────────
\usepackage{listings}
\lstset{
  basicstyle=\fontsize{11}{13}\ttfamily,
  breaklines=true,
  numbers=left,
  numberstyle=\tiny,
  frame=single,
  tabsize=4
}

% ── Hyperlinks (PDF bookmarks) ────────────────────────────────
\usepackage[
  unicode,
  colorlinks=false,
  pdfborder={0 0 0}
]{hyperref}

% ── Bibliography: APA 7th edition ────────────────────────────
\usepackage[
  backend=biber,
  style=apa,
  sorting=nyt
]{biblatex}
\addbibresource{references.bib}

% ── Utilities ────────────────────────────────────────────────
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{enumitem}
% GOST list punctuation: items end with semicolon except last (full stop)
\setlist[itemize]{noitemsep, topsep=4pt}
\setlist[enumerate]{noitemsep, topsep=4pt}

% ============================================================
%  METADATA — fill in before compiling
% ============================================================
\newcommand{\ThesisTitleEN}{<<TITLE_EN>>}
\newcommand{\ThesisTitleRU}{<<TITLE_RU>>}
\newcommand{\AuthorName}{<<AUTHOR>>}
\newcommand{\SupervisorName}{<<SUPERVISOR>>}
\newcommand{\DefenceYear}{<<YEAR>>}
\newcommand{\ProgramCode}{7M06106}
\newcommand{\ProgramName}{Software Engineering}
\newcommand{\University}{Kazakh-British Technical University}

% ============================================================
%  DOCUMENT
% ============================================================
\begin{document}

% ── Title page ───────────────────────────────────────────────
\thispagestyle{nonum}
\begin{titlepage}
  \centering
  \fontsize{14}{16}\selectfont
  \MakeUppercase{\University}\\[6pt]
  School of Information Technology\\[24pt]

  \fontsize{16}{18}\selectfont\bfseries
  \MakeUppercase{\ThesisTitleEN}\\[6pt]
  \MakeUppercase{\ThesisTitleRU}\\[24pt]

  \fontsize{14}{16}\selectfont\mdseries
  Master's Thesis\\
  Programme \ProgramCode{} "\ProgramName"\\[48pt]

  \begin{flushleft}
    \textbf{Author:} \AuthorName\\[6pt]
    \textbf{Supervisor:} \SupervisorName\\[6pt]
  \end{flushleft}

  \vfill
  Almaty, \DefenceYear
\end{titlepage}

% ── Abstract (3 languages, page counted, not numbered) ───────
\thispagestyle{nonum}
\input{tex/abstract}
\clearpage

% ── Table of contents ────────────────────────────────────────
\thispagestyle{nonum}
\tableofcontents
\clearpage

% ── Page numbering starts here ───────────────────────────────
\setcounter{page}{4}   % adjust if front matter is longer
\pagestyle{fancy}

% ── Introduction ─────────────────────────────────────────────
\chapter*{Introduction}
\addcontentsline{toc}{chapter}{Introduction}
\input{tex/introduction}

% ── Chapter 1 ────────────────────────────────────────────────
\chapter{Analytical Review}
\input{tex/chapter_1_review}

% ── Chapter 2 ────────────────────────────────────────────────
\chapter{Methodology}
\input{tex/chapter_2_methodology}

% ── Chapter 3 ────────────────────────────────────────────────
\chapter{Implementation and Results}
\input{tex/chapter_3_implementation}

% ── Chapter 4 ────────────────────────────────────────────────
\chapter{Discussion}
\input{tex/chapter_4_discussion}

% ── Conclusion ───────────────────────────────────────────────
\chapter*{Conclusion}
\addcontentsline{toc}{chapter}{Conclusion}
\input{tex/conclusion}

% ── References ───────────────────────────────────────────────
\printbibliography[heading=bibintoc, title={References}]

% ── Appendices ───────────────────────────────────────────────
\appendix
\chapter{Source Code Excerpts}
% \input{tex/appendix_a}

\end{document}
